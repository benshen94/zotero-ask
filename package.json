{
  "name": "zotero-ask",
  "version": "0.2.3",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check bootstrap.js && node --check core.js"
  },
  "dependencies": {
    "katex": "0.18.7"
  }
}
